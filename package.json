{
  "name": "sns-parse-platforms-repo",
  "private": true,
  "version": "0.0.0",
  "description": "sns-parse 平台定义 monorepo：@sns-parse/platform-*（27）与 @sns-parse/platforms 聚合",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@12.3.4"
}
